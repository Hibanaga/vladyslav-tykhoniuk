'use client'

import React, { useState, useEffect, FC } from 'react';

import StyledComponent from './styles';
import PortfolioDescription from '@/components/modules/PortfolioDescription';

interface Props {
}

const ModuleCaseStudyList: FC<Props> = ({  }) => {
    return (
        <StyledComponent className='module-case-study-list'>
        </StyledComponent>
    );
};

export default ModuleCaseStudyList;
