'use client';

import styled from "styled-components";

export default styled.main`
    margin: 2em 0;
    padding: 0;
    
    .container {
        display: grid;
        grid-template-columns: 1fr 1fr;
        column-gap: 1em;
    }
`
