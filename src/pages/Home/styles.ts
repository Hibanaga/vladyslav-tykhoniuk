'use client';

import styled from "styled-components";

export default styled.main`
    margin: 0;
    padding: 0;
    position: relative;
    
    .container {
        display: grid;
        grid-template-columns: 1fr 1fr;
        column-gap: 1em;
    }
`
